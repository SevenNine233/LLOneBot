FROM node:24-alpine3.23
WORKDIR /app/llbot
COPY dist/ .
RUN node -e "const fs = require('fs'); const s = fs.statSync('llbot.js'); console.log('llbot.js size:', s.size, 'bytes'); if(s.size < 1000) { process.exit(1); }"
